// exemplo simples de soma
function somar(a:number, b:number){
    return a + b
}
// criando a função e exibindo o resultado
const numero1 = 5
const numero2 = 3
console.log(`A soma de ${numero1} e ${numero2} é ${somar(numero1, numero2)}`)